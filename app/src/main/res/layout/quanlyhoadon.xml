<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Tiêu đề -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Quản Lý Hóa Đơn Và Tính Tiền Điện"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="16dp"/>

    <!-- Tìm kiếm hóa đơn -->
    <EditText
        android:id="@+id/edtSearchInvoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Tìm kiếm theo mã hóa đơn, mã điện kế, kỳ"
        android:padding="12dp"
        android:inputType="text"
        android:drawableLeft="@android:drawable/ic_menu_search"/>

    <!-- Danh sách hóa đơn -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvInvoiceList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="16dp" />

    <!-- Nút lập hóa đơn mới -->
    <Button
        android:id="@+id/btnCreateInvoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Lập Hóa Đơn Mới"
        android:layout_marginTop="16dp"
        android:backgroundTint="@color/design_default_color_secondary_variant"
        android:drawableLeft="@android:drawable/ic_menu_add"/>

    <!-- Nút tính tiền điện -->
    <Button
        android:id="@+id/btnCalculateBill"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tính Tiền Điện"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/design_default_color_secondary_variant"
        android:drawableLeft="@android:drawable/ic_menu_edit"/>

    <!-- Nút xem chi tiết hóa đơn -->
    <Button
        android:id="@+id/btnViewInvoiceDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Xem Chi Tiết Hóa Đơn"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/design_default_color_secondary_variant"
        android:drawableLeft="@android:drawable/ic_menu_info_details"/>

    <!-- Nút xóa hóa đơn -->
    <Button
        android:id="@+id/btnDeleteInvoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Xóa Hóa Đơn"
        android:layout_marginTop="8dp"
        android:backgroundTint="@color/design_default_color_secondary_variant"
        android:drawableLeft="@android:drawable/ic_menu_delete"/>

</LinearLayout>